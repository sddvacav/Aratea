# Run 002 — `<TBD market>`

*Builder log. Status: pre-run. To be updated as positions move.*

## Pre-run

- **Market** — `<TBD>` on Kalshi. Question: *`<TBD>`*. Resolution: NWS, `<TBD time UTC>`.
- **Why this market** — `<TBD: 1–2 lines on why this event was picked. Edge thesis, not bullishness.>`
- **Meta-ensemble vote** — ECMWF + GraphCast + GFS + JMA → `p_yes = <TBD>`, confidence `<TBD>`.
- **Per-model** — `<TBD: optional table of individual p_yes per model, useful for post-mortem.>`
- **Climatology baseline** — `p_yes_climatology = <TBD>`. Source: `<TBD>`.
- **Market snapshot at entry** — mid `<TBD>`, bid `<TBD>`, ask `<TBD>`, ts `<TBD UTC>`.
- **Edge** — vs market `<TBD bps>`, vs climatology `<TBD bps>`.
- **Position** — side `<TBD yes|no>`, size `<TBD USD>`, paper.

## Resolution

*To be filled once the market resolves.*

- **Outcome** — `<TBD yes|no>` at `<TBD ts UTC>`.
- **Brier — model** — `<TBD>`.
- **Brier — climatology** — `<TBD>`.
- **P&L** — `<TBD USD>`.
- **Notes** — `<TBD: anything that helps a future reader make sense of this run.>`

## Trace

- Tag — `run-002` (pushed at `<TBD>`).
- Auto-announce — Discord + X via `.github/workflows/announce-release.yml`.
- Manual posts — `<TBD: list of timestamps + canal if post_to_discord.py was used>`.
- Commits — `<TBD: short SHAs of the pre-run, resolution, and update commits>`.

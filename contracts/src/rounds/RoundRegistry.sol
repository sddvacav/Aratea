// SPDX-License-Identifier: Apache-2.0
pragma solidity 0.8.24;

/// @title RoundRegistry — placeholder (Phase 1 / Milestone M3 will fill this in)
/// @notice Real implementation arrives in M3. Will manage propose / challenge / execute / cancel
///         lifecycle for monthly mint rounds, enforcing the 10% monthly cap on-chain.
contract RoundRegistry {
    string public constant PLACEHOLDER = "M3 will replace this with the real round lifecycle + cap enforcement.";
}

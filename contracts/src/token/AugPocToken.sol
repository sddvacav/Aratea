// SPDX-License-Identifier: Apache-2.0
pragma solidity 0.8.24;

/// @title AugPocToken — placeholder (Phase 1 / Milestone M1 will fill this in)
/// @notice Real implementation arrives in M1. This stub exists only so the project compiles
///         and CI runs from M0 onwards. See contracts/README.md for the planned spec.
contract AugPocToken {
    string public constant PLACEHOLDER = "M1 will replace this with the real ERC20 + AccessControl + Pausable.";
}
